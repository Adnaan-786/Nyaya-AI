alembic==1.18.5
annotated-doc==0.0.4
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
certifi==2026.7.22
cffi==2.1.0
click==8.4.2
cryptography==49.0.0
detect-installer==0.1.0
distro==1.9.0
dnspython==2.8.0
docstring_parser==0.18.0
ecdsa==0.19.2
email-validator==2.3.0
fastapi==0.140.0
# Invoice PDF generation (app/services/invoicing.py imports `fpdf`, the module name
# this package installs as). Missing here meant a fresh install could never actually
# send an invoice — found while verifying Phase 3, unrelated to it.
fpdf2==2.8.7
fastapi-cli==0.0.32
fastapi-cloud-cli==0.22.2
fastar==0.11.0
greenlet==3.5.4
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
Jinja2==3.1.6
jiter==0.16.0
Mako==1.3.12
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
packaging==26.2
passlib==1.7.4
pluggy==1.6.0
pyasn1==0.6.4
pycparser==3.0
pydantic==2.13.4
pydantic-extra-types==2.11.1
pydantic-settings==2.14.2
pydantic_core==2.46.4
Pygments==2.20.0
pypdf==6.14.2
pytest==9.1.1
pytest-asyncio==1.4.0
python-dotenv==1.2.2
python-jose==3.5.0
python-multipart==0.0.32
PyYAML==6.0.3
rich==15.0.0
rich-toolkit==0.20.3
rignore==0.8.0
rsa==4.9.1
ruff==0.16.0
sentry-sdk==2.66.1
shellingham==1.5.4
six==1.17.0
sniffio==1.3.1
SQLAlchemy==2.0.51
starlette==1.3.1
typer==0.27.0
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.51.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1.1

# FCM HTTP v1 push (B.8). `requests` is not used by application code — it is
# google-auth's default transport for the token exchange, and google-auth raises a
# confusing ImportError without it.
google-auth==2.56.2
requests==2.32.5

# Object storage (B.9) — imported lazily in app/integrations/storage.py, only once
# S3_BUCKET is actually set. Also the client for any S3-compatible provider (R2, etc.)
# via S3_ENDPOINT_URL.
boto3==1.43.62

# Deliberately absent: pytesseract, pillow, pdf2image. They are the local OCR fallback
# in app/integrations/ocr/tesseract.py, and they are useless without the tesseract-ocr
# and poppler-utils *system* packages, which the Render Python runtime does not carry.
# Installing the Python halves alone would turn a clear "OCR is not configured" into a
# TesseractNotFound at extraction time. The provider names both packages when it
# declines, so a host that does have them only needs `pip install` to light it up.

# DOCX generation for AI drafts (app/ai/docx_generator.py imports `docx`, the module
# name this package installs as). lxml comes with it as its XML backend.
python-docx==1.2.0
lxml==6.1.2
