*.iml
.gradle/
# Kotlin's build scratch dir (session files, compiler error dumps) — not source.
.kotlin/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild
.cxx
*.apk
*.aab
*.keystore
*.jks
!debug.keystore
google-services.json
!app/src/mock/google-services.json
# Unanchored on purpose: the signing config reads `android/keystore.properties`, and a
# leading slash here would only ignore one at the repo root — leaving the real one, with
# the upload key's password in it, tracked.
keystore.properties

# Server
.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.ruff_cache/

# Local Postgres cluster. Lives in the project rather than /tmp, which macOS wipes —
# losing the whole dev database along with it.
.pgdata/
